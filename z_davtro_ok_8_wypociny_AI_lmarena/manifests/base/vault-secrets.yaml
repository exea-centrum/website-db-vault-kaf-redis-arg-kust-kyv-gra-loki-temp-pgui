apiVersion: v1
kind: ConfigMap
metadata:
  name: vault-init
  namespace: davtrowebdbvault
  labels:
    app: website-db-vault-kaf-redis-arg-kust-kyv-gra-loki-temp-pgui
    app.kubernetes.io/name: website-db-vault-kaf-redis-arg-kust-kyv-gra-loki-temp-pgui
    app.kubernetes.io/instance: website-db-vault-kaf-redis-arg-kust-kyv-gra-loki-temp-pgui
data:
  init-vault.sh: |
    #!/bin/bash
    sleep 10
    export VAULT_ADDR="http://vault:8200"
    export VAULT_TOKEN="root"
    
    # Enable KV secrets engine
    vault secrets enable -path=secret kv-v2
    
    # Create database secrets
    vault kv put secret/database/postgres       postgres-user="webuser"       postgres-password="testpassword"       postgres-db="webdb"       postgres-host="postgres-db"
    
    # Create Redis secrets
    vault kv put secret/redis       redis-password=""
    
    # Create Kafka secrets  
    vault kv put secret/kafka       kafka-brokers="kafka:9092"
    
    # Create Grafana secrets
    vault kv put secret/grafana       admin-user="admin"       admin-password="admin"
    
    # Create PgAdmin secrets
    vault kv put secret/pgadmin       pgadmin-email="admin@example.com"       pgadmin-password="adminpassword"
    
    echo "Vault initialization completed"
